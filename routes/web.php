<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider and all of them will
| be assigned to the "web" middleware group. Make something great!
|
*/

// Route::get('/', function () {
//     return redirect()->route('generate.create');
// });

// Route::prefix('/')->controller(App\Http\Controllers\PeopleInformationController::class)->group(function () {
//     Route::get('generate', 'create')->name('generate.create');
//     Route::post('store', 'store')->name('generate.store');
//     Route::get('/show/{md5_id}/{name}', 'show')->name('name.show');
// });
